# HMS codes — outliers (non-hex IDs)

HMS.json entries whose `alarmId` does not match the `0x<hex>` pattern followed by the bulk of the catalog.

| Alarm ID | Tip | Notes |
|---|---|---|
| `unknown` | Contact your local dealer or DJI Support | Literal string `unknown` — not a hex code. Tip is a generic fallback. |

---

**Source**: [`DJI_Cloud/HMS.json`](../../DJI_Cloud/HMS.json).
